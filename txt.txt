import { useEffect,useState } from "react";
import { deleteHandler } from "../../services/services";
import Input from "../ui/Input";
import UpdateModal from "../ui/UpdateModal";



export default function IndexUser(){
    const [message, setMessage] = useState([]);
    const [username, setUsername] = useState('');
    const [password, setPassword] = useState('');
    const [users, setAllUsers] = useState([])
    const userHandler = async ()=>{
        if(username.trim() == "" && password.trim()==""){
            setMessage("Input the username and password");
        }else{
             const response = await fetch('http://localhost:5000/api/Addusers',({
            body:JSON.stringify({username,password}),
            method:"POST",
            headers:{
                "Content-Type":"application/json"
            }
        }))
        const res = await response.json()
        setUsername('')
        setPassword('')
        }
       
    }

    const deleteUser = async (id)=>{
        const result = await deleteHandler(id);
        console.log(result.message)
    }

    useEffect(()=>{
         const getAllUsers = async()=>{
        const response = await fetch('http://localhost:5000/api/allUsers')
        const result = await response.json()
        setAllUsers(result)
    }
    getAllUsers();
    })
   
    
    return(
        <>
            

















            {/* <div className="flex flex-col space-y-10">
            <p>{message}</p>
            <Input value={username}  onChangeText={setUsername}/>
            <Input value={password} onChangeText={setPassword}/>
            <button className="relative active:bg-white active:text-black  border-white inline border bg-black rounded-md text-white hover:-top-[1px]" onClick={()=>userHandler()}>Save User</button>

            <table>
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Password</th>
                    <th>Actions</th>
                </tr>
                  </thead>
                <tbody>

          
                {users.map((user)=>{
                    return(
                        <tr key={user.id}>
                            <td>{user.id}</td>
                            <td>{user.username}</td>
                            <td>{user.password}</td>
                            <td><button className="bg-black border-white border text-white hover:bg-white hover:text-black hover:border-black rounded-[5px] p-[1px]" onClick={()=>deleteUser(user.id)}>Delete</button> <UpdateModal/></td>
                        </tr>
                    )
                })}
                      </tbody>
            </table>
            </div>
            */}
        </>
  
     
    );

}